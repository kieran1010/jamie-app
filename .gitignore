node_modules/
.next/
out/
build/
# Every local env file, however it was created — `vercel env pull` writes
# several different names, and any of them may hold a production connection
# string. The example file is the one deliberate exception.
.env*
!.env.example
*.log
.DS_Store
next-env.d.ts
tsconfig.tsbuildinfo
